services:
    app:
        build: .
        ports:
            - "3333:3333"
        volumes:
            - .:/usr/src/app
        environment:
            - NODE_ENV=development
            - API_PORT=$API_PORT
            - API_URL=$API_URL
        command: ["pnpm", "run", "dev"]
